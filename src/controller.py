"""
DEPRECATED - superseded by control.py (Task 1.3 on/off state machine).

The earlier draft used a 14-16 C band centred on 15 C and an ACH-based flow
idea, both of which the project brief flags as wrong. Nothing imports this
module; it is kept only because the file cannot be deleted in this environment.
"""
